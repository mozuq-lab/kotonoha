# 残作業の索引（2026-09-02 実測。2026-09-05 に Phase 2 完了分を更新、2026-09-06 に Phase 5 完了分を更新）

**この文書は索引であって正本ではない。** 各項目の正本は ADR・`docs/ledger.md`・
`AGENTS.md` にある。ここは「いま何が残っていて、誰が動けば進むか」を1枚で見るためだけの
ものなので、**中身をここに写さない**（写すと4つ目の正本になる）。

**破棄条件**: ADR-007 のリリース条件1〜4がすべて充足した時点で破棄する。

---

## 0. いま止まっているもの

### マージ待ちの自分たちの PR

2026-09-05 時点: #90・#91（条件2・3）、#92（この索引）、#93（Phase 2）はマージ済み。
残るのは #94（Phase 2 計画書の破棄）のみ。

### 滞留している dependabot PR — **16本**

| 種別 | 本数 | 例 |
|---|---|---|
| CI actions | 5 | `codecov-action` 5→6、`upload-artifact` 6→7 ほか |
| frontend (dart) | 5 | `flutter_riverpod` 3.1.0→3.2.1、`go_router` 17.0.1→17.1.0 ほか |
| backend (python) | 5 | `fastapi` 0.124→0.128.6、`anthropic` 0.39→0.79 ほか |

**放置するほど1本あたりの検証コストが上がる。** frontend の2本
（riverpod / go_router）は**リリース前に上げるか、上げないと決めるか**を判断すること
——リリース後に上げると、最初の利用者が乗っているコードから離れる。
backend の5本は Phase 2（PR #93）で `requirements.txt` を書き換えたため全部ずれている。
`sqlalchemy`（#57）は依存自体が消えた。**close して dependabot に作り直させる**。

### 素性の確認が要るブランチ

- **`fix/backend-production-hardening` がリモートに存在しない**（ローカルのみ。2026-09-05 に再確認）。
  **Issue #86 は「このブランチは削除しない。証拠として残す」と決めているが、
  いまこの端末が壊れると消える。** Phase 2 の拾い出し自体は完了した（#86 close）が、
  証拠として残す決定は変わらないので **push して保全すること**（5分で終わる）
- `origin/copilot/add-email-notification-feature` — bot が別リポジトリの変更を誤って push し、
  自分で revert したもの（最終コミット 2025-12-05「Revert all connpass-notifier changes -
  wrong repository」）。**このリポジトリと無関係。削除してよい**

### 閉じ忘れ

- **Issue #84 が open のまま。** 内容（E2E 5本がヘッドレス Web で失敗）は解決済みで、
  残す4経路は CI で緑、`EXCLUDED` は `ai_conversion_e2e_test.dart` だけになっている
  （2026-09-02 実測）。**issue が実態より古い。閉じるだけ**

---

## 1. リリースまで — 残るのは ADR-007 条件4 だけ

条件1（frontend 品質）・条件2（緊急ボタン）・条件3（クラッシュ報告）は充足済み
（PR #89 / #90 / #91）。**正本は `docs/adr/ADR-007-release-criteria.md`。**

| 条件4 の内訳 | 状態 | 動かせるのは |
|---|---|---|
| **開発者登録**（Apple Developer Program / Google Play） | 未着手。**純粋な外部律速で、他の作業を待つ理由が無い** | **人（最優先）** |
| プライバシーポリシーの公開 | 本文は更新済み（#91）。URL は 200 を実測。**GitHub の blob URL を公開先にしてよいか**は未判断 | 人 |
| サポート連絡先の実アドレス | `support@kotonoha-app.example.com` のまま（**RFC 2606 の予約ドメイン＝実在しない**）。台帳 **L-51** | **人** |
| リリース用ビルド（AAB 化＋シンボル） | 未整備。**ADR-009 でストア標準に依拠すると決めた以上、レポートが読めないと決定が意味を持たない**。台帳 **L-52** | エージェント |
| ストア掲載文の点検（医療・治療効果を謳わない） | `fastlane/metadata/` に日英の記述はあるが、**この観点での点検は未実施** | エージェント＋人 |
| 審査通過 | 外部律速 | 人 |

**開発者登録とサポート連絡先の2つが、いま最も長いリードタイムを持つ。**

---

## 2. 是正計画の残り（リリースとは独立に進む）

正本は倉庫の `docs/archive/plans/2026-08-29-architecture-remediation.md`（全フェーズ完了）。

| Phase | 状態 | 備考 |
|---|---|---|
| Phase 2 backend を書き直す | **完了**（PR #93、2026-09-04） | 決定は ADR-002・ADR-006 の改訂節と台帳 #85（D1〜D8、L-53〜L-56）。計画書は #94 で破棄 |
| Phase 4 検査しきれない残余 | **完了**（2026-09-06、Issue #107） | 棚卸しスキル・台帳・層 2 の PR コメント・mutmut を入れ、棚卸しを 1 回回した |
| Phase 5 docs を整理する | **完了**（2026-09-06、PR #109〜#117） | 核 120 行、未卒業 ADR 5 本、正本は README・要件定義・privacy-policy・support・OpenAPI |

---

## 3. 台帳 `docs/ledger.md`

**個別の内容はここに写さない。** 分類だけ。

| 種別 | 例 |
|---|---|
| 記録して受け入れたもの（対応不要） | （却下済み。Phase 5 境界で台帳から削除、履歴は git） |
| 判断が要るもの | L-39（ADR-005 の却下理由と連鎖削除の矛盾）、L-51（サポート連絡先） |
| 条件4 の実作業 | L-52（AAB＋シンボル） |
| その他の残余 | （却下済み。Phase 5 境界で台帳から削除、履歴は git） |

**L-39 は ADR か要件定義の改訂が要る**（テスト緑では反証されない不適合）。
リリースを止めるものではないが、放置すると「決定と実装が食い違ったまま」が続く。

---

## 4. 文書と実態のずれ（2026-09-02 に見つけたもの）

**どれも「文書の数字・記述を、使う前に測り直す」で毎回見つかっている。**

- **AGENTS.md の道具表**: `tsumiki:adr-rubber-duck` は**スキルではなくコマンド**として
  実在する（`commands/adr-rubber-duck.md`）。表はスキルとして書いてあるので、
  そのまま呼ぶと失敗する。**表の点検（月1）が一度も回っていない証拠でもある**
- **ADR-007 の「157件」**: 緊急ボタン関連テストの実測は 176件だった（2026-08-31）
- **fastlane の URL**: 存在しない組織を指して 404 だった（#91 で修正）
- **tech-stack.md の venv 手順**: `uv venv` に `--seed` が無く pip が入らない／pre-commit の導入手順が無い
  （2026-09-05 に venv を作り直して発見。同日修正。README「セットアップ」の `uv venv --seed` で解消）

**Phase 4 の「月1の棚卸し」は 2026-09-06 に整備し、1 回回した**（Issue #107）。
この種のずれを見つける工程は、作業のついでではなく月 1 の工程として存在する。

---

## 提案する順序

1. **`fix/backend-production-hardening` を push して保全する**（5分・失うと取り返せない）
2. **開発者登録を始める**（外部律速。他を待たない）
3. ~~#90 → #91 をマージする~~（済）
4. Issue #84 を閉じる／`copilot/*` ブランチを削除する
5. サポート連絡先を決める（L-51）
6. リリース用ビルドを整える（L-52）＋ストア掲載文を点検する
7. 審査へ出す
8. 以降は Phase 4（棚卸し）へ。Phase 2 は完了。dependabot の backend 分は close して作り直させる
