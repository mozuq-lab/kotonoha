# TASK-0071: 設定画面UI実装 - テストケース定義書

## 概要

設定画面（SettingsScreen）のUIに関するテストケースを定義する。

## 信頼性レベル

🔵 **青信号** - EARS要件定義書およびTASK-0071要件に基づく

## テストケース一覧

### 1. 設定画面表示テスト

| TC-ID | テストケース名 | 対応要件 | 信頼性 |
|-------|---------------|----------|--------|
| TC-071-001 | 設定画面が正常に表示される | FR-071-001 | 🔵 |
| TC-071-002 | AppBarに「設定」タイトルが表示される | FR-071-001 | 🔵 |
| TC-071-003 | 設定画面がスクロール可能である | NFR-071-003 | 🔵 |

### 2. セクション表示テスト

| TC-ID | テストケース名 | 対応要件 | 信頼性 |
|-------|---------------|----------|--------|
| TC-071-004 | 「表示設定」セクションが表示される | FR-071-002 | 🔵 |
| TC-071-005 | 「音声設定」セクションが表示される | FR-071-003 | 🔵 |
| TC-071-006 | 「AI設定」セクションが表示される | FR-071-004 | 🔵 |
| TC-071-007 | セクションは正しい順序で表示される | FR-071-005 | 🔵 |

### 3. 表示設定セクションテスト

| TC-ID | テストケース名 | 対応要件 | 信頼性 |
|-------|---------------|----------|--------|
| TC-071-008 | フォントサイズ設定項目が表示される | FR-071-006, REQ-801 | 🔵 |
| TC-071-009 | フォントサイズ選択肢が3つ表示される（小/中/大） | REQ-801 | 🔵 |
| TC-071-010 | テーマ設定項目が表示される | FR-071-007, REQ-803 | 🔵 |
| TC-071-011 | テーマ選択肢が3つ表示される（ライト/ダーク/高コントラスト） | REQ-803 | 🔵 |

### 4. 音声設定セクションテスト

| TC-ID | テストケース名 | 対応要件 | 信頼性 |
|-------|---------------|----------|--------|
| TC-071-012 | TTS速度設定項目が表示される | FR-071-008, REQ-404 | 🔵 |
| TC-071-013 | TTS速度選択肢が3つ表示される（遅い/普通/速い） | REQ-404 | 🔵 |

### 5. AI設定セクションテスト

| TC-ID | テストケース名 | 対応要件 | 信頼性 |
|-------|---------------|----------|--------|
| TC-071-014 | AI丁寧さレベル設定項目が表示される | FR-071-009, REQ-903 | 🔵 |
| TC-071-015 | AI丁寧さレベル選択肢が3つ表示される（カジュアル/普通/丁寧） | REQ-903 | 🔵 |

### 6. 設定変更テスト

| TC-ID | テストケース名 | 対応要件 | 信頼性 |
|-------|---------------|----------|--------|
| TC-071-016 | フォントサイズを変更できる | REQ-801 | 🔵 |
| TC-071-017 | テーマを変更できる | REQ-803 | 🔵 |
| TC-071-018 | AI丁寧さレベルを変更できる | REQ-903 | 🔵 |

### 7. ウィジェット構造テスト

| TC-ID | テストケース名 | 対応要件 | 信頼性 |
|-------|---------------|----------|--------|
| TC-071-019 | SettingsScreenはconstコンストラクタを持つ | CLAUDE.md | 🔵 |
| TC-071-020 | SettingsScreenはkeyパラメータを受け取れる | CLAUDE.md | 🔵 |

## テストケース詳細

### TC-071-001: 設定画面が正常に表示される

```dart
testWidgets('TC-071-001: 設定画面が正常に表示される', (tester) async {
  await tester.pumpWidget(
    ProviderScope(
      overrides: [/* モック設定 */],
      child: const MaterialApp(home: SettingsScreen()),
    ),
  );
  await tester.pump();

  expect(find.byType(SettingsScreen), findsOneWidget);
  expect(find.byType(Scaffold), findsOneWidget);
});
```

### TC-071-004: 「表示設定」セクションが表示される

```dart
testWidgets('TC-071-004: 表示設定セクションが表示される', (tester) async {
  await tester.pumpWidget(/* SettingsScreen */);
  await tester.pump();

  expect(find.text('表示設定'), findsOneWidget);
});
```

### TC-071-008: フォントサイズ設定項目が表示される

```dart
testWidgets('TC-071-008: フォントサイズ設定項目が表示される', (tester) async {
  await tester.pumpWidget(/* SettingsScreen */);
  await tester.pump();

  expect(find.text('フォントサイズ'), findsOneWidget);
  expect(find.text('小'), findsOneWidget);
  expect(find.text('中'), findsOneWidget);
  expect(find.text('大'), findsOneWidget);
});
```

### TC-071-010: テーマ設定項目が表示される

```dart
testWidgets('TC-071-010: テーマ設定項目が表示される', (tester) async {
  await tester.pumpWidget(/* SettingsScreen */);
  await tester.pump();

  expect(find.text('テーマ'), findsOneWidget);
  expect(find.text('ライト'), findsOneWidget);
  expect(find.text('ダーク'), findsOneWidget);
  expect(find.text('高コントラスト'), findsOneWidget);
});
```

### TC-071-014: AI丁寧さレベル設定項目が表示される

```dart
testWidgets('TC-071-014: AI丁寧さレベル設定項目が表示される', (tester) async {
  await tester.pumpWidget(/* SettingsScreen */);
  await tester.pump();

  expect(find.text('丁寧さレベル'), findsOneWidget);
  expect(find.text('カジュアル'), findsOneWidget);
  expect(find.text('普通'), findsWidgets); // TTS速度にも「普通」があるため
  expect(find.text('丁寧'), findsOneWidget);
});
```

### TC-071-016: フォントサイズを変更できる

```dart
testWidgets('TC-071-016: フォントサイズを変更できる', (tester) async {
  await tester.pumpWidget(/* SettingsScreen */);
  await tester.pump();

  // 「大」ボタンをタップ
  await tester.tap(find.text('大'));
  await tester.pump();

  // 選択状態が変更されていることを確認
  // (実装に応じて検証方法を調整)
});
```

## テストファイル構成

| ファイル | 役割 |
|---------|------|
| `test/features/settings/presentation/settings_screen_test.dart` | 設定画面メインテスト（既存拡張） |
| `test/features/settings/presentation/widgets/font_size_settings_widget_test.dart` | フォントサイズ設定テスト（新規） |
| `test/features/settings/presentation/widgets/theme_settings_widget_test.dart` | テーマ設定テスト（新規） |
| `test/features/settings/presentation/widgets/ai_politeness_settings_widget_test.dart` | AI丁寧さレベル設定テスト（新規） |
| `test/features/settings/presentation/widgets/settings_section_widget_test.dart` | セクションウィジェットテスト（新規） |

## 更新履歴

- 2025-11-29: テストケース定義書作成（TASK-0071）
