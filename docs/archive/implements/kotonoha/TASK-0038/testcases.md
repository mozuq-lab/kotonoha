# TDDテストケース定義書 - TASK-0038: 文字入力バッファ管理（Riverpod StateNotifier）

## タスク情報

- **タスクID**: TASK-0038
- **タスク名**: 文字入力バッファ管理（Riverpod StateNotifier）
- **対象コンポーネント**: `InputBufferNotifier` (StateNotifier)
- **テストファイル**: `frontend/kotonoha_app/test/features/character_board/providers/input_buffer_provider_test.dart`
- **作成日**: 2025-11-22
- **フェーズ**: Phase 3 - Week 9, Day 2

## 信頼性レベル凡例

- **:blue_circle: 青信号**: 要件定義書（EARS記法）・設計文書に基づく確実なテスト
- **:yellow_circle: 黄信号**: 要件定義書から妥当な推測によるテスト
- **:red_circle: 赤信号**: 要件定義書にない推測によるテスト

---

## 1. 正常系テスト

### TC-001: 初期状態テスト - 空文字列の確認

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-001 |
| **テスト名** | InputBufferNotifierの初期状態が空文字列であることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-002, interfaces.dart |
| **前提条件** | ProviderContainerが新規作成された状態 |
| **テストステップ** | 1. ProviderContainerを作成する<br>2. inputBufferProviderの状態を読み取る |
| **期待結果** | 状態が空文字列 `''` である |

---

### TC-002: 文字追加テスト - 単一文字の追加

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-002 |
| **テスト名** | addCharacter()で1文字が入力バッファに追加されることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-002 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `addCharacter('あ')` を呼び出す<br>3. 状態を確認する |
| **期待結果** | 状態が `'あ'` である |

---

### TC-003: 文字追加テスト - 連続した複数文字の追加

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-003 |
| **テスト名** | 複数回のaddCharacter()で文字が連続追加されることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-002 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `addCharacter('あ')` を呼び出す<br>3. `addCharacter('い')` を呼び出す<br>4. `addCharacter('う')` を呼び出す<br>5. 状態を確認する |
| **期待結果** | 状態が `'あいう'` である |

---

### TC-004: 文字追加テスト - ひらがな・カタカナ・漢字の混在

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-004 |
| **テスト名** | ひらがな、カタカナ、漢字を連続追加できることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-002 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `addCharacter('あ')` を呼び出す<br>3. `addCharacter('ア')` を呼び出す<br>4. `addCharacter('亜')` を呼び出す<br>5. 状態を確認する |
| **期待結果** | 状態が `'あア亜'` である |

---

### TC-005: 文字削除テスト - 最後の1文字削除

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-005 |
| **テスト名** | deleteLastCharacter()で最後の1文字が削除されることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-003 |
| **前提条件** | 入力バッファに `'こんにちは'` が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('こんにちは')` で初期状態を設定<br>3. `deleteLastCharacter()` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が `'こんにち'` である |

---

### TC-006: 文字削除テスト - 連続削除

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-006 |
| **テスト名** | 複数回のdeleteLastCharacter()で文字が連続削除されることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-003 |
| **前提条件** | 入力バッファに `'あいう'` が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あいう')` で初期状態を設定<br>3. `deleteLastCharacter()` を2回呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が `'あ'` である |

---

### TC-007: 全消去テスト - バッファクリア

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-007 |
| **テスト名** | clear()で入力バッファが全消去されることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-004 |
| **前提条件** | 入力バッファに `'おはようございます'` が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('おはようございます')` で初期状態を設定<br>3. `clear()` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が空文字列 `''` である |

---

### TC-008: setText テスト - テキスト設定

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-008 |
| **テスト名** | setText()でバッファ内容を設定できることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-002 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('ありがとう')` を呼び出す<br>3. 状態を確認する |
| **期待結果** | 状態が `'ありがとう'` である |

---

### TC-009: setText テスト - 既存テキストの上書き

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-009 |
| **テスト名** | setText()で既存テキストが上書きされることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-002 |
| **前提条件** | 入力バッファに `'古いテキスト'` が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('古いテキスト')` で初期状態を設定<br>3. `setText('新しいテキスト')` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が `'新しいテキスト'` である |

---

### TC-010: setText テスト - 空文字列での設定（クリア目的）

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-010 |
| **テスト名** | setText('')で空文字列を設定できることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-004 |
| **前提条件** | 入力バッファに `'テスト'` が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('テスト')` で初期状態を設定<br>3. `setText('')` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が空文字列 `''` である |

---

### TC-011: 状態通知テスト - Riverpod stateの自動更新確認

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-011 |
| **テスト名** | 状態変更がRiverpod stateに即座に反映されることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | NFR-003 (100ms以内応答), dataflow.md |
| **前提条件** | ProviderContainerが作成された状態 |
| **テストステップ** | 1. ProviderContainerを作成する<br>2. inputBufferProviderを監視開始する<br>3. `addCharacter('あ')` を呼び出す<br>4. 監視結果を確認する |
| **期待結果** | 状態変更が監視側に通知される |

---

## 2. 異常系テスト

### TC-012: 空文字追加の防止

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-012 |
| **テスト名** | addCharacter('')で空文字列を追加しようとしても無視されることを確認 |
| **信頼性レベル** | :yellow_circle: |
| **関連要件** | requirements.md「制約条件」 |
| **前提条件** | 入力バッファに `'あいう'` が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あいう')` で初期状態を設定<br>3. `addCharacter('')` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が `'あいう'` のまま変化しない |

---

### TC-013: 空バッファでの削除（安全処理）

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-013 |
| **テスト名** | 空バッファでdeleteLastCharacter()を呼び出してもエラーにならないことを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | AC-009 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. 初期状態が空であることを確認<br>3. `deleteLastCharacter()` を呼び出す<br>4. 状態を確認する |
| **期待結果** | エラーが発生せず、状態が空文字列 `''` のまま |

---

### TC-014: 空バッファでの全消去（安全処理）

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-014 |
| **テスト名** | 空バッファでclear()を呼び出してもエラーにならないことを確認 |
| **信頼性レベル** | :yellow_circle: |
| **関連要件** | AC-009類推 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. 初期状態が空であることを確認<br>3. `clear()` を呼び出す<br>4. 状態を確認する |
| **期待結果** | エラーが発生せず、状態が空文字列 `''` のまま |

---

### TC-015: 制御文字（改行）の入力防止

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-015 |
| **テスト名** | addCharacter()で改行文字を追加しようとしても無視されることを確認 |
| **信頼性レベル** | :yellow_circle: |
| **関連要件** | requirements.md「エラー1: 制御文字の入力」 |
| **前提条件** | 入力バッファに `'あいう'` が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あいう')` で初期状態を設定<br>3. `addCharacter('\n')` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が `'あいう'` のまま変化しない |

---

### TC-016: 制御文字（タブ）の入力防止

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-016 |
| **テスト名** | addCharacter()でタブ文字を追加しようとしても無視されることを確認 |
| **信頼性レベル** | :yellow_circle: |
| **関連要件** | requirements.md「エラー1: 制御文字の入力」 |
| **前提条件** | 入力バッファに `'あいう'` が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あいう')` で初期状態を設定<br>3. `addCharacter('\t')` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が `'あいう'` のまま変化しない |

---

## 3. 境界値テスト

### TC-017: 1000文字ちょうどの入力 - 境界値下限

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-017 |
| **テスト名** | 999文字の状態から1文字追加で1000文字になることを確認 |
| **信頼性レベル** | :yellow_circle: |
| **関連要件** | EDGE-101 |
| **前提条件** | 入力バッファに999文字が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あ' * 999)` で999文字を設定<br>3. `addCharacter('い')` を呼び出す<br>4. 状態の文字数を確認する |
| **期待結果** | 状態の文字数が1000文字である |

---

### TC-018: 1000文字制限 - 境界値上限での追加拒否

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-018 |
| **テスト名** | 1000文字の状態から文字追加しても追加されないことを確認 |
| **信頼性レベル** | :yellow_circle: |
| **関連要件** | EDGE-101, AC-011 |
| **前提条件** | 入力バッファに1000文字が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あ' * 1000)` で1000文字を設定<br>3. `addCharacter('い')` を呼び出す<br>4. 状態の文字数を確認する |
| **期待結果** | 状態の文字数が1000文字のまま（追加されない） |

---

### TC-019: setText での1001文字切り捨て

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-019 |
| **テスト名** | setText()で1001文字以上を設定しても1000文字で切り捨てられることを確認 |
| **信頼性レベル** | :yellow_circle: |
| **関連要件** | EDGE-101, AC-012 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あ' * 1001)` を呼び出す<br>3. 状態の文字数を確認する |
| **期待結果** | 状態の文字数が1000文字である（切り捨て） |

---

### TC-020: setText での大量文字（2000文字）切り捨て

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-020 |
| **テスト名** | setText()で2000文字を設定しても1000文字で切り捨てられることを確認 |
| **信頼性レベル** | :yellow_circle: |
| **関連要件** | EDGE-101, AC-012 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あ' * 2000)` を呼び出す<br>3. 状態の文字数を確認する |
| **期待結果** | 状態の文字数が1000文字である（切り捨て） |

---

### TC-021: 1文字のみの状態から削除

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-021 |
| **テスト名** | 1文字のみの状態からdeleteLastCharacter()で空になることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-003 |
| **前提条件** | 入力バッファに `'あ'` が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あ')` で1文字を設定<br>3. `deleteLastCharacter()` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が空文字列 `''` である |

---

### TC-022: 1000文字から全消去

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-022 |
| **テスト名** | 1000文字の状態からclear()で空になることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-004, EDGE-101 |
| **前提条件** | 入力バッファに1000文字が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あ' * 1000)` で1000文字を設定<br>3. `clear()` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が空文字列 `''` である |

---

## 4. エッジケーステスト

### TC-023: 絵文字の追加（サロゲートペア）

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-023 |
| **テスト名** | 絵文字が1文字として正しく追加されることを確認 |
| **信頼性レベル** | :red_circle: |
| **関連要件** | AC-013 (要件定義書にない推測) |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `addCharacter('😀')` を呼び出す<br>3. 状態を確認する |
| **期待結果** | 状態が `'😀'` である |

---

### TC-024: 絵文字の削除（grapheme cluster単位）

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-024 |
| **テスト名** | 絵文字がdeleteLastCharacter()で1文字として削除されることを確認 |
| **信頼性レベル** | :red_circle: |
| **関連要件** | AC-013 (要件定義書にない推測) |
| **前提条件** | 入力バッファに `'あ😀い'` が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あ😀い')` で初期状態を設定<br>3. `deleteLastCharacter()` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が `'あ😀'` である |

---

### TC-025: 複合絵文字（家族絵文字）の取り扱い

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-025 |
| **テスト名** | 複合絵文字が1文字として正しく削除されることを確認 |
| **信頼性レベル** | :red_circle: |
| **関連要件** | AC-013 (要件定義書にない推測) |
| **前提条件** | 入力バッファに複合絵文字が含まれている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('あ👨‍👩‍👧い')` で初期状態を設定<br>3. `deleteLastCharacter()` を2回呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が `'あ'` である（複合絵文字が1文字として削除される） |

---

### TC-026: 濁点・半濁点付き文字の追加

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-026 |
| **テスト名** | 濁点・半濁点付き文字が正しく追加されることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-002 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `addCharacter('が')` を呼び出す<br>3. `addCharacter('ぱ')` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が `'がぱ'` である |

---

### TC-027: 半角数字・記号の追加

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-027 |
| **テスト名** | 半角数字・記号が正しく追加されることを確認 |
| **信頼性レベル** | :yellow_circle: |
| **関連要件** | REQ-002類推 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `addCharacter('1')` を呼び出す<br>3. `addCharacter('!')` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が `'1!'` である |

---

### TC-028: 全角数字・記号の追加

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-028 |
| **テスト名** | 全角数字・記号が正しく追加されることを確認 |
| **信頼性レベル** | :yellow_circle: |
| **関連要件** | REQ-002類推 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `addCharacter('１')` を呼び出す<br>3. `addCharacter('！')` を呼び出す<br>4. 状態を確認する |
| **期待結果** | 状態が `'１！'` である |

---

### TC-029: 連続操作テスト - 追加→削除→追加

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-029 |
| **テスト名** | 追加、削除、追加の連続操作が正しく動作することを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-002, REQ-003 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `addCharacter('あ')` を呼び出す<br>3. `addCharacter('い')` を呼び出す<br>4. `deleteLastCharacter()` を呼び出す<br>5. `addCharacter('う')` を呼び出す<br>6. 状態を確認する |
| **期待結果** | 状態が `'あう'` である |

---

### TC-030: 連続操作テスト - 全消去後の追加

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-030 |
| **テスト名** | 全消去後に文字追加ができることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | REQ-002, REQ-004 |
| **前提条件** | 入力バッファに `'テスト'` が設定されている |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `setText('テスト')` で初期状態を設定<br>3. `clear()` を呼び出す<br>4. `addCharacter('新')` を呼び出す<br>5. 状態を確認する |
| **期待結果** | 状態が `'新'` である |

---

### TC-031: Provider定義テスト - inputBufferProviderの型確認

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-031 |
| **テスト名** | inputBufferProviderがStateNotifierProvider<InputBufferNotifier, String>であることを確認 |
| **信頼性レベル** | :blue_circle: |
| **関連要件** | architecture.md, requirements.md「Providerの定義」 |
| **前提条件** | なし |
| **テストステップ** | 1. inputBufferProviderの型を確認する<br>2. ProviderContainerから読み取った値の型を確認する |
| **期待結果** | Providerの状態型がStringである |

---

### TC-032: 複数文字列の連続addCharacter（2文字以上の文字列）

| 項目 | 内容 |
|------|------|
| **テストケースID** | TC-032 |
| **テスト名** | addCharacter()に2文字以上の文字列を渡した場合の動作を確認 |
| **信頼性レベル** | :yellow_circle: |
| **関連要件** | requirements.md「制約: 1回の呼び出しで1文字のみ追加」 |
| **前提条件** | 入力バッファが空の状態 |
| **テストステップ** | 1. InputBufferNotifierを取得する<br>2. `addCharacter('あい')` を呼び出す（2文字）<br>3. 状態を確認する |
| **期待結果** | 最初の1文字のみが追加される（`'あ'`）、または全体が無視される |

---

## 5. テストカバレッジマトリクス

| テストケースID | 正常系 | 異常系 | 境界値 | エッジケース | 関連要件 |
|---------------|--------|--------|--------|------------|----------|
| TC-001 | x | | | | REQ-002 |
| TC-002 | x | | | | REQ-002 |
| TC-003 | x | | | | REQ-002 |
| TC-004 | x | | | | REQ-002 |
| TC-005 | x | | | | REQ-003 |
| TC-006 | x | | | | REQ-003 |
| TC-007 | x | | | | REQ-004 |
| TC-008 | x | | | | REQ-002 |
| TC-009 | x | | | | REQ-002 |
| TC-010 | x | | | | REQ-004 |
| TC-011 | x | | | | NFR-003 |
| TC-012 | | x | | | 制約条件 |
| TC-013 | | x | | | AC-009 |
| TC-014 | | x | | | AC-009類推 |
| TC-015 | | x | | | 制約条件 |
| TC-016 | | x | | | 制約条件 |
| TC-017 | | | x | | EDGE-101 |
| TC-018 | | | x | | EDGE-101 |
| TC-019 | | | x | | EDGE-101 |
| TC-020 | | | x | | EDGE-101 |
| TC-021 | | | x | | REQ-003 |
| TC-022 | | | x | | REQ-004 |
| TC-023 | | | | x | AC-013 |
| TC-024 | | | | x | AC-013 |
| TC-025 | | | | x | AC-013 |
| TC-026 | | | | x | REQ-002 |
| TC-027 | | | | x | REQ-002類推 |
| TC-028 | | | | x | REQ-002類推 |
| TC-029 | | | | x | REQ-002, REQ-003 |
| TC-030 | | | | x | REQ-002, REQ-004 |
| TC-031 | x | | | | architecture.md |
| TC-032 | | x | | | 制約条件 |

---

## 6. 要件カバレッジ

| 要件ID | テストケースID | カバレッジ |
|--------|---------------|-----------|
| REQ-002 | TC-002, TC-003, TC-004, TC-008, TC-009, TC-026, TC-027, TC-028, TC-029, TC-030 | 100% |
| REQ-003 | TC-005, TC-006, TC-021, TC-029 | 100% |
| REQ-004 | TC-007, TC-010, TC-022, TC-030 | 100% |
| EDGE-101 | TC-017, TC-018, TC-019, TC-020, TC-022 | 100% |
| NFR-003 | TC-011 | 部分的（パフォーマンス測定は実装テストで詳細化） |
| AC-009 | TC-013, TC-014 | 100% |
| AC-010 | TC-012 | 100% |
| AC-011 | TC-018 | 100% |
| AC-012 | TC-019, TC-020 | 100% |
| AC-013 | TC-023, TC-024, TC-025 | 100% |

---

## 7. 次のステップ

### 推奨コマンド

次は `/tsumiki:tdd-red` でテストコード（失敗するテスト）を作成します。

### テストファイル配置

```
frontend/kotonoha_app/test/
└── features/
    └── character_board/
        └── providers/
            └── input_buffer_provider_test.dart  # ← 作成対象
```

### 実装ファイル配置（Green フェーズで作成）

```
frontend/kotonoha_app/lib/
└── features/
    └── character_board/
        └── providers/
            └── input_buffer_provider.dart  # ← Green フェーズで作成
```

---

## 更新履歴

- **2025-11-22**: テストケース定義書作成
  - 要件定義書（requirements.md）に基づいて32個のテストケースを洗い出し
  - 正常系11件、異常系5件、境界値6件、エッジケース10件
  - 信頼性レベル（:blue_circle::yellow_circle::red_circle:）を各テストケースに付与
  - 要件カバレッジマトリクスを作成
