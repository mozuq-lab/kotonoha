# 台帳 — 後回しにした問題と判断待ち

規則: 1 問題 1 行＋出所。経緯は書かない。`[ ]` 未対応 ／ `[x]` 対応済み ／ `[-]` 却下。
毎月の棚卸しで見直し、`[x]` と `[-]` は行ごと消す（履歴は git。消した行は `git log -p -S 'L-NN' -- docs/ledger.md` で引ける）。レビューに渡すのは `[ ]` だけ。
読むのは必要なときだけ（核には数えない）。#85（この台帳の PR のマージ後に閉鎖）から仕分けて作った。

## 未対応
- [ ] L-25 `frontend/kotonoha_app/integration_test/device_test/`（実機QA手順、1,773行）が一度も実行されていない — ADR-007 条件 4（ストア提出前に必要）
- [ ] L-51 サポート連絡先が `support@kotonoha-app.example.com` のまま（RFC 2606 の予約ドメイン） — docs/support.md, docs/privacy-policy.md。ADR-007 条件 4
- [ ] L-52 Android のアップロード鍵が無い（AAB・mapping・シンボルは #97 で解決済み） — .github/workflows/release.yml。ADR-007 条件 4。開発者登録後。Android の release ビルドは署名鍵が無いと debug 鍵で署名される（`frontend/kotonoha_app/android/app/build.gradle.kts:67-73`）
- [ ] L-55 AI 変換の平均応答時間（3秒以内）が未測定 — ADR-002 のプロバイダ支出上限設定と同日に実測（backend 公開の前提）
- [ ] L-57 Android 12 以上と iOS の実機で、OS のバックアップから履歴・定型文・お気に入り・設定が復元されることを確認 — NFR-106（2026-09-12 に #99 の除外を撤回）
- [ ] L-59 `exc.errors(include_input=False, ...)` から `include_input=False` を落とす mutant が生存 — ValidationError の入力値が `ConfigError` へ漏れないことを検査するテストが無い（ADR-003） — backend/app/config.py:161（mutmut 生存。#106 の測定の残り）
- [ ] L-60 `SafeError.__init__` の `super().__init__(code.value)` を `None` にする mutant が生存 — 基底 `SafeError` の文字列表現が `ErrorCode` を保持することを検査するテストが無い — backend/app/errors.py:108（mutmut 生存。#106 の測定の残り）
- [ ] L-61 `RateLimiter.__init__` の `RateLimitItemPerSecond(times, seconds)` の `seconds` を `None` にする mutant が生存 — 設定した秒数がレート制限ウィンドウ長に反映されることを検査するテストが無い — backend/app/ratelimit.py:42（mutmut 生存。#106 の測定の残り）
- [ ] L-65 mutmut の対象テストは pyproject の 4 ファイル指定で固定されており、テストを増やしても走らない（kill rate が理由なく下がる）。`also_copy` で app/ 全体を写して指定を無くせるか試す — backend/pyproject.toml [tool.mutmut]
- [x] L-66 lib内参照0のLogLevel enumと専用testを削除済み。AppLoggerは保持 — 棚卸し2026-09、L-139の決定を束3で適用（2026-09-20）。
- [ ] L-67 お気に入りが `frontend/kotonoha_app/lib/features/favorite/`（ロジック）と `frontend/kotonoha_app/lib/features/favorites/`（UI）の 2 ディレクトリに分かれたまま — ADR-005（棚卸し 2026-09 で現存を確認、Phase 5（文書）の後）
- [ ] L-68 要件 ID 107 件のうち 14 件が、テストにも openspec にも 1 度も現れない（追跡性の穴。挙動は別 ID で試験済みのものを含む） — docs/spec/kotonoha-requirements.md（一覧は棚卸し 2026-09 の PR 本文）
- [ ] L-69 `expect(widget.runtimeType.toString(), equals('CharacterBoardWidget'))` はクラス名を固定するだけで挙動を検査しない — frontend/kotonoha_app/test/widgets/character_board_optimization_test.dart:389（棚卸し 2026-09、観点 4）
- [x] L-73 入力欄の 1000 文字上限に警告表示が無く、超過分を黙って捨てる（EDGE-101 の未達） — frontend/kotonoha_app/lib/features/character_board/providers/input_buffer_provider.dart:65,89（PR #114 の突き合わせ）。解消（2026-09-16、#133: 入力欄の直下に告知。持ち越しは L-109）
- [x] L-74 フォントサイズ設定が home_screen 配下にしか届かず、定型文一覧が追従しない（REQ-802・REQ-2007 の未達） — frontend/kotonoha_app/lib/features/preset_phrase/presentation/widgets/phrase_list_item.dart:98、frontend/kotonoha_app/lib/app.dart:47-56（PR #114 の突き合わせ）。解消（2026-09-16、テーマの textTheme とボタンテーマに倍率を掛ける。PR は下記）
- [ ] L-75 `docs/store-assets-guide.md` と `frontend/kotonoha_app/integration_test/device_test/README.md` は正本ではなく実行可能な手順として現在地に残す。ADR-007 条件 4（ストア提出）の充足で倉庫へ — Phase 5 A1
- [ ] L-76 NFR-502（backend の重要なビジネスロジック・API エンドポイントで 90% 以上のカバレッジ）が未測定。CI の `fail_under` は全体閾値（NFR-501） — docs/spec/kotonoha-requirements.md:166（L-55 と同形）。基準が 3 か所にある（pyproject の fail_under 80、flutter.yml の THRESHOLD 80、codecov.yml の python 90% と存在しないパスの除外）。決定（2026-09-20 第 3 回、決定シート）: カバレッジ基準を 1 箇所に寄せてから NFR-502 を測る（A）。ADR-005 の 1 概念 1 真実と同じ形。どれが正かが決まらないと、測った数字が何に照らされるのかが決まらない
- [x] L-77 libから呼ばれないshowOfflineAIConversionDialog・TextInputFieldと対応test、未使用exportを削除済み — PR #114の突き合わせ、L-139の決定を束3で適用（2026-09-20）。
- [ ] L-78 README.md:82 の `curl -s localhost:8000/api/v1/health` は、localhost が ::1 に解決される環境で timeout する（uvicorn は 127.0.0.1 で待つ）。127.0.0.1 に直すか注記する — Task 6 の実測（2026-09-06）
- [ ] L-84 開発者登録（Apple Developer Program / Google Play）が未着手。外部律速で、他の作業を待つ理由が無い — ADR-007 条件 4（人が動かす。旧 remaining-work.md から移記）
- [ ] L-85 ストア掲載文（`frontend/kotonoha_app/fastlane/metadata/ja-JP`・`en-US`）を「医療・治療効果を謳わない」観点で点検していない — ADR-007 条件 4（旧 remaining-work.md から移記）
- [ ] L-91 コードや CI が守っているのに ADR に無い決定: `/health` の無認証・無レート制限（`backend/app/routes.py:68-75`、`backend/tests/contract/test_health_docs.py`）、環境名による認証省略と `/docs` 公開（`backend/app/config.py:106-117`）、CORS の形（`backend/app/main.py:187-193`）、`patch('app.` 禁止（`backend/scripts/gates.sh:15-16`）、lint の設定値（`backend/pyproject.toml`）。次の棚卸しで作成条件 3 つに照らして ADR にするか決める — 2026-09-12 の監査（第二線）。決定（2026-09-20 第 2 回、決定シート）: 現状維持（A）。10 月の棚卸しで作成条件 3 つに照らす。未卒業 ADR は 5 本の上限に達しており、足すなら何かを卒業させる判断が先
- [ ] L-92 GitHub Actions 12 種が可変メジャータグ（`@v2` 等）で固定され SHA ピン留めが無い。秘密（AI_API_KEY・VERCEL_TOKEN・ANDROID_*・APPLE_*）を扱う job で走る — .github/workflows/*.yml（2026-09-12 の監査）。決定（2026-09-20 第 3 回、決定シート）: SHA にピン留めする（A）。供給網の固定は規律 8 の「依存の追加」と同じ扱い。dependabot が既に actions を見ている（#66〜#70）ので、ピン留めしても更新は自動で届く。自作の検査は作らない（既存の権威の出力を消費する。規律 8 の②）
- [ ] L-94 `release.yml` が署名鍵・証明書・p12 パスワードを workspace のファイルに書き出す（`:100-121`、`:181-186`）。層 2 の照合に workflow を足したのは 2026-09-12 — .github/workflows/release.yml。決定（2026-09-20 第 3 回、決定シート）: 直す（A）。ランナーの一時ディレクトリに置き `if: always()` で消す。**後始末が 1 つも無いことを実測した**（`rm -f` も `if: always()` も `release.yml` に存在しない。2026-09-20）。tag でしか走らないので現状の被害は小さいが、仕組みが無い状態。L-52（アップロード鍵の配置）と同じ束で
- [x] L-95 Hive の box 名の正が `frontend/kotonoha_app/lib/core/persistence/persistence_state.dart:31-35` にあり、`hive_init.dart` を触らずに変えられる（層 2 の永続化の照合外） — 2026-09-12 の監査。解消（2026-09-20、**すでに検査があった**。`frontend/kotonoha_app/test/core/persistence/hive_schema_allowlist_test.dart` の検査 C が `PersistedArea.boxName` を直接読む。`'history'` を `'historyX'` に変える変異で赤（`Which: at location ['history'] is 'historyX' instead of 'history'`）、復元して 5 件緑を実測。2026-09-12 の監査より後に検査が入ったのに、この行が更新されていなかった）
- [x] L-96 読み込まれないfrontend/kotonoha_app/web/.env.exampleを削除済み。規律8の設定キー削除はPR本文にyesと根拠を記録 — 2026-09-12の監査、L-139の決定を束3で適用（2026-09-20）。
- [ ] L-97 pytest が third-party 由来の非推奨警告 2 件を出す（fastapi/starlette の TestClient、anyio の別名）。出力が静かでない — backend、#120
- [ ] L-98 ストア提出の前に独立監査（規律 9、棚卸しスキルの付録）を 1 回行い、結果を台帳へ書く。条件 4 の充足判定の前 — AGENTS.md 規律 9、ADR-007 条件 4
- [ ] L-99 定型文由来のお気に入りの重複判定が `sourceId` だけなので、★付きの定型文を削除して同文を作り直し★を押すと同文のお気に入りが 2 件並ぶ（#125 で連鎖削除を外して開いた経路。お気に入り画面から消せる）。content でも判定するかは判断待ち — frontend/kotonoha_app/lib/features/favorite/providers/favorite_provider.dart:242、ADR-005 限界。決定（2026-09-20 第 2 回、決定シート）: 記録して受け入れる（A）。お気に入り画面から消せる。content でも判定する案は「同じ文の別の定型文」を持てなくする副作用があり、説明しづらい規則が増えるので採らない（僅差）
- [ ] L-100 `resetToDefaults()` は lib からの呼び出しゼロの死蔵コードで、呼ばれると定型文由来のお気に入りを全件 dangling にする — frontend/kotonoha_app/lib/features/preset_phrase/providers/preset_phrase_notifier.dart:292（#125 の独立監査、棚卸し観点 1）。決定（2026-09-20 第 2 回、決定シート）: 消す（A）。L-139 と同じ「呼び出し 0 のコードを残さない」。束 3 で実施
- [x] L-102 作り直しと保存失敗が同時だと作り直しの告知が失敗の告知に置き換わる／退避→削除の後に再オープンが失敗すると削除の事実が伝わらない — frontend/kotonoha_app/lib/core/persistence/persistence_state.dart:119-130、lib/core/utils/hive_init.dart:159-182（#126 の独立監査）。前半（作り直し・救出の告知が失敗の告知に置き換わる）は解消（2026-09-19: 保存できない状態のとき、バナーが起動時の破損の結果を直接読んで並べる。#139 の独立監査と Codex がどちらも最重要と評価）。**残るのは後半**（退避 → 削除の後に再オープンが失敗すると、削除の事実が伝わらない）。**後半も解消**（2026-09-20、空にしたことを開き直す前に伝える。退避と削除が済んだ時点で「以前のデータはこの box に無い」は確定するので、開き直しの成否に結びつけない。伝える時点をテストで固定した）
- [x] L-103 常設バナーの文字はアプリ設定の 3 段階フォントに追随しない（`fontSize: 14` 固定。OS の文字拡大は効く） — frontend/kotonoha_app/lib/core/widgets/persistence_banner.dart（#126 の独立監査。L-74 と同系）。解消（2026-09-16、#134: バナーの文字 14px に設定の倍率を掛ける。中では 14px のまま。一度 bodyMedium にしたが、20px になり画面を圧迫するので撤回した）
- [x] L-104 下書き（`draft_text`）とチュートリアル完了フラグの SharedPreferences 書き込みは失敗しても報告されない（try も無く未処理の非同期エラーになる）。下書きは NFR-302 の対象で「最もコストの高い損失」とコード自身が書く — frontend/kotonoha_app/lib/features/app_state/providers/app_session_provider.dart:112-150、lib/features/help/providers/tutorial_provider.dart:60-71（#127 の独立監査）。解消（2026-09-16、#135: 設定と同じ報告経路に流しバナーに「入力中の文を保存できません」。再起動後に消失の理由を残せない限界は ADR-005 のとおり残る）
- [ ] L-106 ADR-005 が #125〜#127 で 55 行まで肥大化した（挙動の細部・日付・台帳番号を追記）。60 行上限も「改訂は追記せず書き直す」の規則も 2 系統レビューも捕まえず、利用者が adr-touch のコメントで気づいた。#128 で構造を変えた（ADR-010:34 の 1 句、規律 7 のレンズ）。10 月の棚卸し観点 6 で、以後の ADR 差分に細部が入らなかったかを見て、効かなければ数字（上限）を見直す — docs/adr/ADR-010-document-framework.md:34、AGENTS.md 規律 7
- [ ] L-107 ストア提出用の画像がゼロ（アイコン 1024/512、iPhone・iPad・スマートフォンのスクリーンショット、Play のフィーチャーグラフィック 1024×500）。`docs/store-assets-guide.md` のチェックリストは全項目が未着手 — frontend/kotonoha_app/fastlane/（画像 0 件、2026-09-16 実測）。ADR-007 条件 4
- [ ] L-109 入力上限の告知（#133）の持ち越し: (1) `setText` 経由（AI 変換結果）の 1000 文字超は切り詰められ、告知は「達した」と「切り詰めた」を区別しない（定型文は上限 500、候補は短文なので現実の経路は AI 変換結果だけ）(2) `substring` は UTF-16 単位で切るためサロゲートペアを壊しうる（文字盤から絵文字は入力できない）(3) 標準レイアウトで告知表示時のレイアウトテストが無い — frontend/kotonoha_app/lib/features/character_board/providers/input_buffer_provider.dart:84、#133 の 2 系統レビュー。告知文のフォント追従は #134 のテーマ倍率で解消を確認（大で bodyMedium×1.2）。決定（2026-09-20 第 2 回、決定シート）: (1) 告知で「達した」と「切り詰めた」を区別する＝直す／(3) 標準レイアウトの告知表示テストを足す＝直す／(2) サロゲートペアは**記録して受け入れる**（文字盤から絵文字を入力できず到達経路が無い。規律 8）
- [x] L-110 緊急ボタンの E2E（`large_emergency_buttons_test.dart` TC-E2E-084-021）が headless web で時々「はい」の直後に緊急画面を見つけられない（#133 の CI run 34997386337。main の直近 3 回は緑）。`startEmergency()` が音声再生の await の後に `alertActive` を書くため、再生が遅いと緊急表示が遅れる。視覚を先（state を先に書く）にして音声を後にすれば利用者にも E2E にも効くが、決定として出す — frontend/kotonoha_app/lib/features/emergency/presentation/providers/emergency_state_provider.dart:71-82。解消（2026-09-19、視覚を先に決定。開始もリセットも state を先に書き、音声サービスが再生開始・停止を呼ばれた順に 1 本ずつ実行する。再生を 3 秒遅らせた E2E で赤→緑）
- [ ] L-111 フォント設定の追従はテーマの倍率がけで実装したが、これは「テーマに明示サイズがあるスタイル」（bodyLarge・bodyMedium・titleLarge とボタン）にしか効かない。サイズ無しのスタイル（ダイアログの見出し headlineSmall、bodySmall 等）は Theme.of の localize で既定サイズが後から入るため倍率の外で、REQ-2007「すべてのテキスト要素」は満たしていない。根治は MediaQuery の textScaler をアプリ根で掛け、文字盤等の 3 つの `switch (fontSize)` を撤去する方式（16/20/24 = 20×0.8/1.0/1.2 なので見た目は同じ。約 10 ファイル）。決定（2026-09-19）: 根治は今はやらず、緊急確認ダイアログだけ先に直した（固定の `AppTextStyles` のサイズをテーマの bodyMedium から取る。根治で textScaler に移るときはテーマの倍率がけ `_scaled` を同時に外すこと。併存させるとここも二重掛けになる）。追従しない固定サイズはほかに、`AppTextStyles` が `ai_conversion_result_dialog.dart` の 9 箇所、リテラルの `fontSize:` が 11 ファイル 13 箇所（`error_dialog.dart`・`tts_button.dart`・`volume_warning_widget.dart` ほか。2026-09-19 に `git grep` で計数。倍率を掛けている `persistence_banner.dart` を除く） — frontend/kotonoha_app/lib/core/themes/theme_provider.dart、Task 2 のレビュー
- [x] L-112 緊急確認ダイアログの「いいえ」「はい」は、画面幅 375 以下（スマホ縦持ち）で縦積みになり、間隔が 0 になる（幅 360 で「いいえ」の下端 501.5 ＝「はい」の上端 501.5、376 から横並び。フォント設定によらない）。取り消しと実行が接するので誤タップしうる。`actionsOverflowButtonSpacing` が未指定で、間に入れた `SizedBox(width: 8)` は縦積みでは高さ 0 — frontend/kotonoha_app/lib/features/emergency/presentation/widgets/emergency_confirmation_dialog.dart の actions（#137 の 2 系統レビュー、2026-09-19 実測。今回の変更の前から）。解消（2026-09-20、`actionsOverflowButtonSpacing: 16` と `buttonPadding: horizontal 24`（`spacing` はその半分で 24）で縦・横を別々に宣言し、間の空 `SizedBox` は撤去した。2 系統のレビューが独立に、空の箱は幅が折り返し判定にも紛れ込むこと、PR 本文の却下理由が誤っていること（同プロパティは横に効かない）を指摘した。幅 320/360/375/376/768/1024 で並び方と隙間の両方を見る形に赤→緑、幅 360・800 の実機（Chrome）でも確認）
- [x] L-130 誤操作防止の「16px 以上離す」基準が、全消去の確認ダイアログに当たっていない。`ClearConfirmationDialog` の actions は間隔の指定が無く、`AlertDialog` の既定のまま**横 8px**（`TextButton` が細いので縦積みにはならず 0 にはならない）。消した入力は戻せず、利用者は打ち直しもできない。REQ-5002 は緊急ボタンと全消去を並べて誤操作防止の対象にしているのに、基準が呼び出し側に散っていて共有されていない — frontend/kotonoha_app/lib/features/character_board/presentation/widgets/clear_confirmation_dialog.dart:30-57（#148 の 2 系統レビュー、2026-09-20）。**8px は倍率 1.0 のときだけで、文字設定「大」と OS の文字拡大が重なると L-112 と同じ 0px に達する**（定型文の削除は幅 320 なら倍率 1.3、幅 360/375 なら 2.0 で 0.0。全消去は幅 320・倍率 2.4 で 0.0。お気に入りの削除は幅 320/360 が倍率 2.0、375 が 2.4 で 0.0。2026-09-20 実測）。対象は **9 箇所**（緊急呼び出し・全消去・定型文の削除・お気に入りの削除（個別と全件）・履歴の削除・入力欄の置き換え・AI 変換の同意・**定型文の追加**・**定型文の編集**）で、どれも間隔を宣言していなかった。決定（2026-09-20）: 呼び出し側がラベルだけを渡す `ConfirmationDialog` を 1 つ置き、並び（間隔・並び順・スクロール）をそこだけで決める。7 箇所まで解消（2026-09-20、2 本に分けて実施。お気に入りと履歴にあった重複の `_ConfirmDialog` は撤去した。お気に入りの削除は移行前 横 8.0／縦積み 0.0 → 移行後 24.0／16.0 を実測）。残り 2 箇所（定型文の追加・編集）は本文がフォームなので `ConfirmationDialog` に入らず、`ConfirmationDialogLayout` を渡す形で解消（幅 320・倍率 1.3 と幅 360・倍率 2.0 で 0.0 を実測。#149/#150 の 2 系統レビューが発見）。**9 箇所すべて解消**
- [x] L-131 緊急確認ダイアログは `scrollable` が未指定（既定 false）なので、フォント設定「大」と OS の文字拡大が重なると内容テキストが画面からあふれる。幅 360×640・合成倍率 2.4 で `RenderFlex overflowed by 139 pixels`（修正前）。L-112 の修正で縦積みの隙間が 16px 増えるぶん、あふれ量も 139→155 のように 16px 増える（2026-09-20 実測）。**ボタンは全設定で画面内・ヒット可能**で、減るのは説明文の見える量。あふれ始める倍率は修正の前後で変わらない。根治は L-111 の `textScaler` 方式か `scrollable: true` — frontend/kotonoha_app/lib/features/emergency/presentation/widgets/emergency_confirmation_dialog.dart:143（#148 の 2 系統レビュー、2026-09-20）。一部解消（2026-09-20、`scrollable: true`。actions は `SingleChildScrollView` の外に残るのでボタンは常に見えたまま、title と本文がスクロールする。幅 360×640・倍率 2.4 で 237px の溢れ →溢れなし、倍率 3.2 で 632px →溢れなしを実測）。`ConfirmationDialog` と緊急呼び出しに入れたので、誤操作防止ダイアログ 7 箇所すべてに行き渡った

- [x] L-132 `ConfirmKind.normal` の実行ボタン（AI 変換の同意）は、背景 #2196F3 とダイアログ面 #F5F5F5 の比が **2.87:1** で、WCAG 2.1 の非テキストコントラスト（1.4.11）の 3:1 を下回る。文字は 6.72:1 で AA を満たすので読めるが、ボタンの輪郭が面から浮かない。**移行前は塗っていなかったので #150 が作った状態**。ダーク 3.62 / 高コントラスト 21.00 は問題なし、`destructive` は 3 テーマとも 3:1 以上 — frontend/kotonoha_app/lib/shared/widgets/confirmation_dialog.dart（#150 の 2 系統レビュー、2026-09-20 実測）。決定（2026-09-20、決定シート）: 枠線を足して非テキスト 3:1 を満たす（A）。塗りは保ち、テーマの色は変えない。束 1 で実施。解消（2026-09-20、塗りが面に対して 3:1 に**届かないときだけ**枠線で境界を作る形にした。塗りの色は変えないので、足りている `destructive` の見た目は変わらない。3 テーマ × 2 kind で境界の比を見る検査を足した）
- [x] L-133 `showNetworkErrorDialog`（キャンセル ＋ 再試行の二択）は、**倍率 1.0 のスマホ縦持ちでも actions が右にあふれる**。幅 320 で `RenderFlex overflowed by 56 pixels on the right` かつ間隔 0.0、幅 375 でも 1px あふれる。倍率 2.4 では 326px。再試行は取り消せない実行ではないので L-130 の対象外だが、ボタンが切れて押せない形は別の問題 — frontend/kotonoha_app/lib/core/widgets/error_dialog.dart:165-196（#150 の 2 系統レビュー、2026-09-20 実測）。束 1 で直す（決定不要）。解消（2026-09-20、あふれの正体は **title の `Row` に `Expanded` が無い**ことだった（見出しが折り返せず右へ出る）。ネットワーク・AI 変換・読み上げの 3 つに足した（`showErrorDialog` は元から持っていた）。あわせて 4 つとも `ConfirmationDialogLayout.build` に通し、縦積みで 0px に接していた間隔も直した。4 ダイアログ × 幅 4 × 倍率 4 ＝ 64 本が赤→緑）
- [ ] L-134 デスクトップのブラウザ（`defaultTargetPlatform` が macOS / Windows / Linux）では `ThemeData` が `materialTapTargetSize: shrinkWrap` と `VisualDensity(-2,-2)` を選ぶため、テーマが指定した `minimumSize` 44×44 から 8 引かれて **`TextButton` が 36px になる**（REQ-3001 の 44px 未満）。確認ダイアログの「いいえ」で実測。**main からの既存の穴でアプリ全体の `TextButton` に及ぶ**（#150 は「はい」を 36→52 に改善しただけ）。契約テストはテストの既定プラットフォーム（Android 相当）で回るので見張れていない。テーマで `visualDensity: VisualDensity.standard` を指定するか、`materialTapTargetSize` を固定するかの決定が要る — frontend/kotonoha_app/lib/core/themes/light_theme.dart（#150 の 2 系統レビュー、2026-09-20 実測）。決定（2026-09-20、決定シート）: テーマで `visualDensity: standard` と `materialTapTargetSize: padded` を固定する（A）。REQ-3001 がどのプラットフォームでも成り立つ。**デスクトップで縦に伸びるので全画面の再確認が要る**。束 3 で実施
- [x] L-135 契約テスト（`confirmation_dialog_contract.dart`）は、**本番の呼び出し元ではなくウィジェットを直接組んで**当てている箇所がある。そのため `send_to_input_button.dart` の `ConfirmationDialog` を素の `AlertDialog` に戻しても 2230 本が全部緑のまま（レビューが実測）。お気に入り・履歴の画面テストの `confirmationButton()` も `ButtonStyleButton` なら何でも拾う。「新しく素の `AlertDialog` を書く」残余リスクは ADR-008 のとおり受け入れるが、**既存の呼び出し元が戻されても気づかない**のは別。本番画面を pump する形に寄せるか、受け入れるかの決定 — frontend/kotonoha_app/test/shared/widgets/confirmation_dialog_test.dart（#150 の 2 系統レビュー、2026-09-20）。解消（2026-09-20、契約を `home` ＋ `open` の形にし、9 経路のうち 8 経路を本番の入口から開くようにした。素の `AlertDialog` に戻す変異 3 種がそれぞれ 16 本の赤になることを確認）。当初 AI 変換の同意だけ未カバーとしたが、`HomeScreen` を pump して到達できた（`pumpAndSettle` が返らないので `settle` の差し替え口をテスト側に足した）。**9 経路すべてが本番の入口から開く。**「新しく素の `AlertDialog` を書く」残余リスクは ADR-008 のとおり受け入れる

- [x] L-136 入力中のフォームが、**端末の戻るボタンで確認なしに閉じて打った文が消える**。定型文の追加・編集で文字盤から入力したあと `handlePopRoute()` を投げると、確認も出ずにダイアログが閉じ、保存もされない（実測: `handled=true dialog=0 confirmShown=0 saved=null`）。`barrierDismissible: false` は**外タップだけ**を塞ぐ。`PopScope` はリポジトリ全体に 1 つも無い（`git grep "PopScope\|canPop\|onWillPop" lib/` が空）。L-130 で間隔を 0→16 にしたのと同じ害に至る別の扉。**Chrome では別の壊れ方**をして、`history.back()` でダイアログだけ残り背後の画面がホームに戻る（実機で確認）。「入力があるなら閉じる前に確認する」を 1 箇所に置く決定が要る — frontend/kotonoha_app/lib/features/preset_phrase/presentation/widgets/phrase_add_dialog.dart ほか（#151 の 2 系統レビュー、2026-09-20 実測）。決定（2026-09-20、決定シート）: 「入力があるなら閉じる前に確認する」を 1 箇所に置く（A、`PopScope`）。定型文の追加・編集に適用。Chrome の「ダイアログだけ残り背後がホームに戻る」がこれで直るかは未確認で、残る可能性がある。束 2 で実施。一部解消（2026-09-20、`DiscardInputGuard` を 1 つ置き、入力があるうちは戻る操作で「入力中の内容を破棄しますか？」を出す形にした。定型文の追加・編集に適用。`handlePopRoute()`（Android のシステムバック相当）で赤→緑、変異 5 種で赤を確認。**ブラウザの戻る（`history.back()`）は直っていない** — L-143 へ分けた）
- [x] L-137 定型文の追加・編集は、**取り消せないのが「キャンセル」側**（打った文が消える）で、編集ではキャンセルも保存も取り消せない。`ConfirmKind.destructive` が置いている「取り消せない実行だけを塗る」という決定がこの 2 つには当てはまらず、保存ボタンは塗っていない。これ自体は正しいが、当てはまらないことがコードにもテストにも書かれていない。重さの伝え方を別に決めるか、書き残すか — frontend/kotonoha_app/lib/features/preset_phrase/presentation/widgets/phrase_add_dialog.dart（#151 の 2 系統レビュー、2026-09-20）。束 1 で「destructive の決定が当てはまらない」ことを実装とテストに書き残す（決定不要）。解消（2026-09-20、「取り消せないのはキャンセル側」であることと「だから保存は塗らない」ことを追加・編集の両方のコードに書いた。**テスト側には何も置いていない**——当初「実装とテストに書き残す」としていた範囲を縮めた。文言なので検査する対象が無いと判断した）
- [x] L-138 定型文の追加・編集のダイアログは、`AlertDialog(scrollable: true)` の中に**自前の `SingleChildScrollView` が残っている**（入れ子）。内側は無限高さ制約で `maxScrollExtent = 0` になりドラッグを取らないので無害だが、効かない仕組みを残さない（ADR-008）。あわせて `scrollable: true` により**見出し「定型文を追加」も一緒にスクロールアウトする**ようになった（倍率 2.4・幅 320）。どこを編集しているか分からなくなる向きなので、title を固定する案もある — frontend/kotonoha_app/lib/features/preset_phrase/presentation/widgets/phrase_add_dialog.dart（#151 の 2 系統レビュー、2026-09-20 実測）。決定（2026-09-20、決定シート）: 見出しがスクロールアウトするのは現状維持（A）。ボタンは常に見えていて、見出しはスクロールで戻せる。あふれを消す利益のほうが大きい。入れ子の `SingleChildScrollView` 撤去は決定不要として束 1 で一緒に実施。解消（2026-09-20、自前の `SingleChildScrollView` を外した。`AlertDialog` の中の `SingleChildScrollView` が 1 つだけであることを見る検査を足した（2 個 → 1 個）。見出しのスクロールアウトは決定どおり現状維持）

- [x] L-139 到達経路0のエラーダイアログ4つ・snackbar系・専用定数と対応testを削除済み — #153 の2系統レビュー、2026-09-20 決定・束3で実施。
- [x] L-140 ボタンの枠線を決めている場所が 2 つある（ADR-005）。`ConfirmationDialog` の `ElevatedButton.styleFrom(side:)` と `high_contrast_theme.dart` の `elevatedButtonTheme.side`。`side: null` はテーマへ委譲し、非 null はテーマを**上書きする**（SDK の `effectiveValue` は `widgetValue ?? themeValue ?? defaultValue`）。いまライト／ダークにテーマ側の side が無いので衝突しないが、足すと「コントラストが足りるときだけテーマの枠線が効く」という読めない挙動になる。テーマ側に寄せれば 1 箇所になる — frontend/kotonoha_app/lib/shared/widgets/confirmation_dialog.dart（#153 の 2 系統レビュー、2026-09-20）。決定（2026-09-20 第 2 回、決定シート）: テーマ側に寄せる（A）。ADR-005 の 1 概念 1 真実。L-141 で閾値の分岐が消えるので `side` はテーマだけが決める形になる。解消（2026-09-20、`ConfirmationDialog` の局所 `side` を撤去し、3 テーマの `ElevatedButtonTheme` に一元化。`confirmation_dialog_test.dart` が本番の全消去・AI 同意の入口で解決済み枠線を確認）
- [x] L-141 **危険度と境界の強さが逆転している。** AI 変換の同意（`normal` ＝ 取り消せる）には枠線が付き、全消去の確認（`destructive` ＝ 取り消せない）には付かない（塗りが 3:1 を満たすため）。誤操作防止が製品の眼目である以上、より危険な方の境界が弱いのは説明しづらい。「常に枠線を引く」形にすれば閾値の分岐ごと消えるが、`destructive` の見た目が変わるので決定が要る。あわせて、枠線の色は**面の輝度だけ**で選んでおり塗りとの比を見ていない（面と塗りが同系の中間輝度になるテーマを足すと黒枠が塗りに埋もれる）— frontend/kotonoha_app/lib/shared/widgets/confirmation_dialog.dart（#153 の 2 系統レビュー、2026-09-20）。決定（2026-09-20 第 2 回、決定シート）: 常に枠線を引く（A）。`destructive` にも枠線が付き見た目が変わるが、誤操作防止が眼目である以上、危険な方の境界が強い形なら説明できる。閾値の分岐ごと消えるので規律 8 の①。枠線の色は面の輝度だけでなく塗りとの比でも選ぶ。解消（2026-09-20、面・塗りとの比を比較して light=黒／dark=白／high contrast=黒をテーマ枠線に選択。3 テーマ × `normal`/`destructive` の本番入口で、塗りが面に 3:1 以上、または枠線が面・塗りの両方に 3:1 以上を測定）
- [x] L-142 同じ 2.87:1（`colorScheme.primary` の塗り × ライトの面）が、`ConfirmationDialog` の外にも残っている。チュートリアルの「次へ」で実機確認。L-132 の直しはウィジェット局所なので届かない。テーマ側で解くか、個別に直すか — frontend/kotonoha_app/lib/core/themes/light_theme.dart（#153 の 2 系統レビュー、2026-09-20 実機確認）。決定（2026-09-20 第 2 回、決定シート）: テーマ側で解く（A）。L-141 の枠線をテーマに置けばチュートリアルの「次へ」を含む全画面に届く。個別対応は #153 が「ウィジェット局所は届かない」と実証した。解消（2026-09-20、実 Chromium のライトテーマで「次へ」の黒枠を確認して 5 step を完走。3 テーマの同ウィジェットは実描画 `FilledButton` の枠線と Card 面を widget test で測定）

- [ ] L-143 **ブラウザの戻る（`history.back()`）は `PopScope` を通らない。** 入力中の定型文ダイアログを開いたまま戻ると、**ダイアログは残ったまま背後の画面だけがホームに戻る**（Chrome・幅 400 で実機確認、2026-09-20）。`canPop: false` を置いても変わらない。go_router がブラウザ履歴を下の page に写すため、ダイアログの route の `PopScope` を経由しない。**訂正（2026-09-20）: 「回復できる」は誤りだった。** 打った文は画面に残るが、その状態で「保存」を押しても**何も起きない**（画面側の `onSave` クロージャが掴んでいる `ref` がunmounted になる。`preset_phrase_screen.dart:159`）。ダイアログは閉じず無反応のままで、出口は「キャンセル」だけ ＝ **結局打った文は失われる**（Chrome 幅 400 で実機確認。一覧に追加されていないことをスクリーンショットで確かめた。main でも同じで、この PR 由来ではない）。L-136（システムバック）は解消済みで、こちらは別経路。**害は L-136 と同じ「打った文が消える」**。go_router 側で受けるか、Web では受け入れるかの決定が要る — frontend/kotonoha_app/lib/core/router/app_router.dart、lib/shared/widgets/discard_input_guard.dart（#153 の 2 系統レビューが発見、束 2 で実機再現）。決定（2026-09-20 第 2 回、決定シート）: 単独では対処しない（A）。L-144 で経路ごと消える。束 B で L-144 と同時に確かめる

- [ ] L-144 「入力が消える」こと自体を**表現不可能にする**案を検討していない（規律 8 の ①）。このアプリは文字盤の入力に対して既に下書きの保存・復元を持っている（`AppLifecycleObserver` → `appSessionProvider.saveDraftText`、400ms デバウンス）。定型文フォームにも同じ仕組みを載せれば、**システムバック・ブラウザの戻る・OS kill・タブを閉じる**の全部が無害になる。`DiscardInputGuard`（#154）が塞ぐのはシステムバック 1 つだけで、L-143 もこれで消える。載せるか、確認ダイアログで足りるとするかの決定 — frontend/kotonoha_app/lib/features/app_state/providers/app_session_provider.dart、lib/features/preset_phrase/presentation/widgets/phrase_add_dialog.dart（#154 の 2 系統レビュー、2026-09-20）。決定（2026-09-20 第 2 回、決定シート）: 定型文フォームにも下書きの保存・復元を載せる（A）。既存の `saveDraftText`（400ms デバウンス、`app_lifecycle_observer.dart` 経由）を再利用するので新規発明が無い。規律 8 の①で、システムバック・ブラウザの戻る・OS kill・タブを閉じるが全部無害になる。束 B で実施

- [ ] L-145 Android のクローズドテストが未実行（代行サービスの選定、12 人以上 × 14 日連続のオプトイン、その後 production access を申請）。申請フォームはテスターの募集のしやすさ・全機能を使ったか・本番利用者の使い方と一致したか・集めたフィードバックの要約と収集方法を問う（2026-09-20 に公式ページで確認）。代行で計数要件は満たせるが、回答は実際に起きたことで書く — ADR-007 条件 4（L-108 の決定。人が動かす。L-84 の開発者登録の後、Android 公開の直前の律速）
- [ ] L-146 Play Console のプライバシーポリシー URL（「アプリのコンテンツ」）は fastlane の metadata に無く、コンソールで設定する（iOS は `privacy_url.txt` が正）。L-86 の差し替えと同じ URL にする — ADR-007 条件 4（人が動かす。開発者登録 L-84 の後）
- [ ] L-147 「経緯は書かない」（本文書 3 行目）に反して伸びた行が裾にある。未対応 55 行のうち 200 字以下は 27 行だが、401 字超が 10 行（最長 839 字の L-125）。上限の数字を置くか決める（`scripts/inventory.sh` は台帳の 3 状態を既に数えている） — ADR-010、L-106 と同形。10 月の棚卸し観点 6。決定（2026-09-20 第 2 回、決定シート）: 10 月の棚卸しで 200 字の上限を ADR-010 の改訂として置く（A）
- [ ] L-148 pre-commit の `Flutter Analyze` hook は PATH 上の Flutter 3.41.5 を使い、指定 SDK 3.38.1 と異なるため `flutter pub get` で `pubspec.lock` を drift させる。3.38.1 の `fvm dart pub get --enforce-lockfile` 後に `fvm flutter analyze --no-pub --no-fatal-infos` は error 0 / warning 0 で通るが、hook を指定 SDK で実行する整備が未了 — 2026-09-20 実行証拠。設定変更の要否を決める

## 判断待ち
- [ ] L-58 の残り: backend 公開の 4 条件（支出上限・デプロイと proxy 段数・端末キー配布・実プロバイダでの往復） — ADR-002。Web 成果物に鍵を焼かない（#121 で外した）。決定（2026-09-20 第 2 回、決定シート）: ストア提出後に回す（A）。ADR-007 が「初回は AI 変換抜き可、リリースは backend に依存しない」と決めている。L-55（応答時間の実測）も同日に
- [ ] L-70 道具表の 4 目的（影響範囲・ADR の引き出し・状態遷移・仕様乖離の逆生成）が未割当になった。tsumiki を有効に戻すか、別の道具を割り当てるか — AGENTS.md 道具表（棚卸し 2026-09 で降ろした）。決定（2026-09-20 第 2 回、決定シート）: 未割当のまま手で行い、10 月の棚卸しで再評価する（A）。tsumiki を戻す案は、9 月の棚卸しで降ろした理由が変わっていないので採らない
- [x] L-80 `frontend/kotonoha_app/pubspec.lock` は Flutter 3.41.5 で解決したまま。`.fvmrc`／CI の 3.38.1 で `flutter pub get` すると `characters` が 1.4.1→1.4.0 に下がり `js` 0.7.2 が加わる。lock を 3.38.1 で作り直すか SDK を上げるか（依存の決定、規律 8） — Task 4 の実測（2026-09-06）。決定（2026-09-20、決定シート）: 3.38.1 で lock を作り直す（A）。手元と CI が一致し、コミットのたびの drift が消える。SDK を 3.41.5 に上げる案は影響が大きいので採らない。別セッション（dependabot）で実施。解消（2026-09-20、3.38.1 で lock を作り直した。SDK ピンの 6 パッケージが下がり `js` 0.7.2 が加わる。旧 lock は `pub get` で 32 行動いたが、新 lock は 0 行でテスト後も 0 行。analyze は info のみ・format 変更なし・2,445 件緑）
- [ ] L-81 `backend/tests/contract/openapi_baseline.json` は正規化ダンプで、単体では現行 API の形しか示さない（enum の値や説明文を含まない）。ADR-010:28 と AGENTS.md 文書節が「API の正本」と呼ぶ記述の限界。呼び方を変えるか、ダンプに補うか — ADR-010「限界」。決定（2026-09-20 第 2 回、決定シート）: 呼び方を変える（A）。実体は契約テストの基準であって仕様書ではない。ADR-010 と AGENTS.md 文書節の 1 句を直す。ダンプに補う案は維持費が増えるので採らない
- [ ] L-82 ADR-007 条件 1 の括弧書き 4 項目が定義か要約か（限界節に「まだ決めていない」と明記） — docs/adr/ADR-007-release-criteria.md:48。決定（2026-09-20 第 2 回、決定シート）: 括弧書き 4 項目を**定義とする**（A）。要約と読むと本来の定義が破棄済みの是正計画の中にしか無く、定義がどこにも残らない。ADR-007 の「条件は 4 つで閉じる」とも整合する（どちらの読み方でも条件 1 は充足済み）
- [x] L-86 プライバシーポリシーの公開先が GitHub の blob URL のまま（`frontend/kotonoha_app/fastlane/metadata/*/privacy_url.txt`。#91 で 200 を実測）。ストア提出の公開先としてこれでよいか — ADR-007 条件 4（旧 remaining-work.md から移記）。決定（2026-09-20、決定シート）: GitHub Pages など安定した公開先に移す（A）。審査で blob URL は弱い。別セッション（ADR-007 改訂）で実施。ADR-007 条件 4 に公開先として明記（#155）。配信の仕組みは #156 （`.github/workflows/pages.yml`。privacy-policy・support・user-guide だけを出し、ADR・台帳・仕様は出さない）。**残り 3 手順**: ① main へマージ ② Pages を Actions ソースで有効化 ③ `https://mozuq-lab.github.io/kotonoha/privacy-policy.html` と `/support.html` の 200 と本文を実測してから `privacy_url.txt`・`support_url.txt`（各 ja-JP / en-US の 4 ファイル）を差し替える。**順序を逆にすると、実測していない URL を正として置くことになる**（blob URL は #91 で 200 を実測済みなので、先に外すと退行）。解消（2026-09-20、#155/#156 のマージ後に実施。Pages を Actions ソースで有効化し、`https://mozuq-lab.github.io/kotonoha/` の index・privacy-policy.html・support.html・user-guide 4 本がすべて 200、support.html のリンク先 5 本も 200、内部文書（ledger・adr・spec・archive）は 404 であることを実測してから、4 ファイルを差し替えた）
- [ ] L-87 dependabot の PR 15 本が滞留（actions #66〜#70、pip #55〜#59、pub #60・#62〜#65）。frontend の `flutter_riverpod` #62・`go_router` #60 はリリース前に上げるか決める。backend の 5 本は Phase 2 で requirements.txt が変わりずれており、`sqlalchemy` #57 は依存自体が無い。close して作り直させるか — .github/dependabot.yml（旧 remaining-work.md から移記）。決定（2026-09-20、決定シート）: actions 5 本（#66〜#70）はまとめてマージ／backend 5 本（#55〜#59）は close して作り直させる（Phase 2 で requirements.txt が変わってずれており、`sqlalchemy` は依存自体が無い）／frontend 4 本は L-80 を決めた後（A）。別セッション（dependabot）で実施
- [ ] L-88 `fix/backend-production-hardening` はローカルにしか無い（#86 で「証拠として残す」と決定）。push して保全するか、ローカル限りとするか — Issue #86（旧 remaining-work.md から移記）。決定（2026-09-20、決定シート）: push して保全する（A）。#86 の「証拠として残す」に沿う
- [x] L-101 Hive 自身の自動復旧（本番既定 `crashRecovery: true`）は末尾の破損を黙って切り捨てて開くため、最も起きやすい形の破損では退避も告知も無い。`crashRecovery: false` に倒して全破損を自前経路（退避＋告知）に集約するか（部分救出を捨てる）、受け入れるか。決定（2026-09-19）: どちらでもなく第 3 案。まず `false` で開き、破損なら退避してから `true` で開き直して読める分を救い、「一部を読み込めませんでした」と伝える（救えなければ従来の作り直し） — frontend/kotonoha_app/lib/core/utils/hive_init.dart:91、hive 2.2.3 storage_backend_vm.dart:91-96（#126 の独立監査）。解消（2026-09-19、第 3 案を実装。`crashRecovery` 引数を消し、退避 → 救出 → 空なら作り直し。実 box で赤→緑、独立監査済み。持ち越しは L-102・L-113〜L-117）
- [x] L-105 `EmergencyStateNotifier.startEmergency()` / `resetEmergency()` が音声再生の await の後に state を書き、headless web の E2E（`large_emergency_buttons_test.dart` TC-E2E-084-013）がテスト完了後の UnmountedRefException で時々赤になる（2026-09-12 の #125 CI、同一コミットの再実行で緑）。await 後に `ref.mounted` を確かめる修正を、遅い FakeAudioService で赤を先に見る単体テスト付きで入れるか、フレークとして残すか — frontend/kotonoha_app/lib/features/emergency/presentation/providers/emergency_state_provider.dart:80。解消（2026-09-13、#131: await 後に `ref.mounted` を確かめる。遅い音声サービスで赤→緑）
- [x] L-108 Google Play の個人アカウント（2023-11-13 以降に作成）は製品版の前にクローズドテスト（12 人以上が 14 日間連続で参加）が必須。ADR-007 は「β 配布」を却下し「オープンテスト段階は挟まない」と決めたが、これはストア側の必須要件で選べない。組織アカウント（D-U-N-S 番号）で登録して要件を外すか、クローズドテストを計画して ADR-007 を改訂するか。L-84 の登録前に決める — https://support.google.com/googleplay/android-developer/answer/14151465（2026-09-16 確認）、ADR-007 条件 4。決定（2026-09-20、決定シート）: クローズドテストを計画して **ADR-007 を改訂する**（A）。12 人以上 × 14 日連続の確保が要る。組織アカウント（D-U-N-S）は取得に時間と費用がかかるため採らない。**L-84 の開発者登録より前に**。別セッション（ADR-007 改訂）で実施。解消（2026-09-20、ADR-007 を改訂: クローズドテストは条件 4「ストア要件」の内側に置き、条件は 4 つのまま閉じる。**iOS を先に本公開し、Android はクローズドテスト通過後に続ける**（削減案 B「Android を外す」の最小形＝外さずに後ろへずらす）。実際の順序は開発者登録 → クローズドテスト → production access 申請 → 公開で、Play Console が要るので L-84 の後になる（「L-84 より前」は*決める*のが先の意味）。実行は L-145）
- [-] L-113 破損の告知（作り直し・救出）はプロセスの寿命にしか無い。バナーを読む前にアプリが終わると、次回起動ではファイルが正常なので二度と出ない。作り直しは空になるので気づけるが、救出は見た目で分からない。告知を次回起動へ持ち越すか（保存先が壊れていた直後に何へ書くか）は決定が要る — frontend/kotonoha_app/lib/main.dart:31-43、lib/core/persistence/recreated_areas_provider.dart（#139 の独立監査）。**その起動だけにする**（2026-09-20、開発者の決定: 次回起動へ持ち越さない。持ち越すには「保存先が壊れていた直後に、告知したことを何へ書くか」を別に決める必要があり、そこも壊れうる。破損の発生率は測る手段が無く（ADR-009 でクラッシュ報告を送らない）、見合う複雑さではないと判断した。見逃した場合は伝わらないまま残る）
- [x] L-114 破損ファイルの退避は 1 世代で、次の破損で上書きされる。しかも旧退避を消してから写すので、写しが失敗（ディスクフル等）すると新旧どちらの退避も残らない（そのとき元のファイルには手を付けない）。L-101 の実装で末尾破損でも退避が走るようになり、頻度が上がった。一時名へ写してから rename にするか、世代を持つか（容量とプライバシーとの兼ね合い） — frontend/kotonoha_app/lib/core/utils/hive_box_backup_io.dart:37-41（#139 の独立監査。上書きは実測、写しの失敗はコード読解）。解消（2026-09-20、開発者の決定: 一時名へ写してから入れ替える。`<box>.hive.corrupt.bak.tmp` に書き切り、長さを確かめ、fsync してから rename する。写しに失敗しても前回の退避が残る。世代は 1 つのまま。**ピーク時の容量は 1 世代ぶん増える**（元＋前回の退避＋一時ファイル）ので、空きが足りない端末では退避に失敗しうる（失敗側は安全で、退避も box も残り告知が出る）。書きかけの一時ファイルは、次に box が正常に開けたときに片付ける（#146 の独立監査と Codex））
- [x] L-115 履歴・お気に入りを全削除しても、退避ファイル `<box>.hive.corrupt.bak` は消えず（lib に消すコードが無い）、OS バックアップにも乗る（Android `allowBackup="true"`、iOS Documents）。過去の発話が残るので、プライバシーポリシーの「個別または全削除」と食い違う。全削除で退避も消すか、ポリシーに書くか。ADR-005 は退避を「支援者が端末を調べるときの保険」とする — docs/privacy-policy.md:69-74、frontend/kotonoha_app/lib/features/history/data/history_repository.dart:70-72（#139 の独立監査。元から。L-101 で頻度が上がった）。ストア提出の前（L-98）に決める。解消（2026-09-19、開発者の決定: 退避は消さず、プライバシーポリシーに書く。「データの保管」に壊れたデータの写しを、「データの削除」にアプリ内の削除では消えないこと・OS バックアップに乗った分はアンインストールでも残ることを日英で記載）
- [-] L-116 長さ 0 の box ファイルは、退避も告知も無く空で開く（定型文は既定文に置き換わる）。Hive は初回にも 0 バイトのファイルを作るので区別できない。電源断でファイルが 0 バイトになるかは未確認。記録して受け入れる候補（規律 8） — hive 2.2.3 frame_helper.dart:18（#139 の独立監査、実測）。**記録して受け入れる**（2026-09-20、開発者の決定: 区別する手段が無いので自作の検出器は作らない。規律 8）
- [x] L-117 `docs/user-guide/faq.md:190` の「クラウドには保存されません」は、OS のバックアップに乗る（NFR-106、privacy-policy.md:66、`allowBackup`）ことと食い違う 。同じ形がほかに 4 箇所: `frontend/kotonoha_app/fastlane/metadata/ja-JP/description.txt:47`「端末内にのみ保存」、`en-US/description.txt:47`、`review_information/notes.txt:16`、`docs/user-guide/troubleshooting.md:251`「自動バックアップ機能…は提供されていません」 — #139 の独立監査、#141 のレビュー（元から）。ストア掲載文を含むので L-85・L-98 と一緒に。解消（2026-09-20、開発者の決定: すべて直す。**独立監査が同じ主張を 7 箇所見つけた**（App Store 掲載文 2・審査メモ・FAQ・troubleshooting に加え、Play 掲載文 `android/fastlane/metadata/android/{ja-JP,en-US}/full_description.txt` の 2 箇所を取りこぼしていた）。「本アプリ自身がクラウドへ同期することはない。OS のバックアップ機能を有効にしていれば対象になる」の趣旨に統一。FAQ は AI 変換の送信を否定しない書き方にした（Codex）。プライバシーポリシーは元から正しい。`docs/archive/` は倉庫なので直さない）
- [x] L-118 プライバシーポリシーの「ローカルに保存されるデータ」の一覧（入力履歴・定型文・お気に入り・アプリ設定）に、入力中の文の下書き（SharedPreferences の `draft_text`。#135 で保存失敗の告知を足した対象）が載っていない。足すかどうか — docs/privacy-policy.md:16-23、frontend/kotonoha_app/lib/core/persistence/settings_write_failure_provider.dart:32（2026-09-19、L-115 の修正中に気づいた。ストア提出の前の監査 L-98 の対象）。解消（2026-09-19、開発者の決定: 足す。一覧に「入力中の文」を日英で追加。入力欄を空にすると消える）
- [x] L-119 履歴・お気に入りの個別削除は、Hive では印のフレームを足すだけで、元のフレームは compaction（削除 60 件超かつ全体の 15% 超）まで `.hive` ファイルに残る（全削除の `clear()` はファイルを 0 に切り詰めるので消える）。アプリからは見えないが、端末のファイルと OS バックアップ、破損時の写しには残る。履歴は上限 50 件のあふれも個別削除なので、最大で 60 件ほどの過去の発話が残りうる。受け入れてポリシーに書くか、個別削除のたびに `compact()` するか — hive 2.2.3 `box_impl.dart:71-79`・`default_compaction_strategy.dart`、frontend/kotonoha_app/lib/features/history/data/history_repository.dart:65,100（#141 のレビュー、2026-09-19 にソースで確認。写しについてはポリシーに記載済み）。解消（2026-09-19、開発者の決定: 個別削除のたびに実際に取り除く。`PersistedBox.delete` が delete → compact → flush、`clear` が clear → flush。書き込みは呼ばれた順に 1 本ずつ実行する（compact と上書き put が重なると、その項目が書き直したファイルに入らないため）。compact の失敗は保存の失敗にせずログに留める。実ファイルのバイト列で赤→緑。履歴 50 件・約 19KB で上限あふれの保存 1 回が中央値 1.3ms。既存の端末のファイルは、更新後の最初の個別削除かあふれで掃除される。過去に取られた OS バックアップと、削除より前に作られた破損時の写しには残る。持ち越しは L-120〜L-123）
- [x] L-120 同じキーへの上書き保存（定型文の編集、お気に入りの並べ替え）では、編集前の内容が同じ box の次の個別削除までファイルに残る（再起動をまたぐ）。L-119 は削除だけを対象にした。上書きのときも compact するか — frontend/kotonoha_app/lib/core/persistence/persisted_box.dart の put、lib/features/preset_phrase/providers/preset_phrase_notifier.dart:149（#142 の独立監査が実測、Codex も指摘。元から）。解消（2026-09-20、開発者の決定にそって上書きのたびの compact はしない。compact は書き直して rename する操作で、fsync の前に電源が落ちると box 全体を失いうる。上書きは入力のたびに起きるので、追記 1 件の喪失で済むところを box 全体に広げてしまう。代わりに**このセッションの最初の書き込みで 1 回だけ**掃除する。古い内容が残る時間は「次に書き込むまで」。ただし掃除が失敗したセッションでは残ったままになる → L-125）
- [-] L-121 compact が一度失敗すると（ディスク満杯など）、その box は再起動まで compact されない（hive 2.2.3 `storage_backend_vm.dart:147-148` の `_compactionScheduled` が成功時にしか戻らない。Hive 自身の自動 compaction も止まる）。その間の削除は成功と報告され、消した内容は次回起動後の最初の削除まで残る。起動時に各 box を 1 回 compact すれば、L-120 と既存の端末の分もまとめて掃除できる（書き込みが重ならない時点なので安全）。やるかどうか — #142 の独立監査（実測）。**却下**（2026-09-20、起動時に別枠で compact する案はやらない。独立監査が実測で示したとおり、掃除の呼び出しを増やすと、その 1 回の失敗が以後の削除の掃除まで止める: `delete reported=[true] stillInFile=true`。代わりに `PersistedBox` がセッションの最初の書き込みで 1 回だけ掃除する（L-120）。「compact が一度失敗するとそのセッションは掃除されない」という問題自体は残る → L-125）
- [ ] L-122 履歴が上限（50 件）のとき、`addHistory` が await されずに 2 回重なると、どちらも同じ最古の 1 件を消して 2 件足すので 51 件になり、その後も 51 件のまま（データは失われない。ポリシーの「最大50件」と 1 件ずれる） — frontend/kotonoha_app/lib/features/history/data/history_repository.dart:49-57、lib/features/character_board/presentation/home_screen.dart:655（#142 の独立監査が実測。元から）。決定（2026-09-20、決定シート）: 直す（A）。`addHistory` の書き込みを直列化する（`PersistedBox` と同じ形）。別セッション（Hive）で実施
- [x] L-123 プライバシーポリシーの「アプリ内の設定から履歴・お気に入りを個別または全削除」は実装と違う（削除は履歴画面とお気に入り画面から。FAQ は合っている）。また、削除した後も、すでに取られた OS バックアップには通常のデータが残ることを書いていない（写しについては #141 で書いた） — docs/privacy-policy.md:75,169、frontend/kotonoha_app/lib/features/history/presentation/history_screen.dart:144,166（#142 の独立監査。元から）。ストア提出の前の監査 L-98 の対象。解消（2026-09-20、日英とも削除の入口を「アプリ内の履歴画面・お気に入り画面から」に直し、**抜けていた定型文の削除**（定型文画面）を足し、すでに取られた OS のバックアップには削除が及ばないことを書いた。あわせて `docs/user-guide/faq.md:139` の「履歴項目を左にスワイプして削除」も実装に合わせた（実装は削除アイコン。lib に `Dismissible` は無い。#145 のレビュー））
- [x] L-124 `hive_init_salvage_test.dart` の「自動復旧でも開けない破損は、退避してから削除し、空で作り直したと伝える」が、後片付けの `Hive.close()` で `unreadable.lock` 不在の `PathNotFoundException` を出すことがある。全テスト初回はこの 1 件が失敗、コード変更なしの単体再実行は 7 件、全体再実行は 2,126 件成功（既存 skip 1）。関係する実装・テストは統合前と main に同一で、今回の競合解消では修正していない — frontend/kotonoha_app/test/core/utils/hive_init_salvage_test.dart:66、hive 2.2.3 storage_backend_vm.dart:215、#142 の競合解消検証（2026-09-20、`fvm flutter test --no-pub --reporter expanded`）。解消（2026-09-20、機序を特定した: `Hive.openBox` が失敗すると hive 2.2.3 は失敗した box の `close()` を await せずに呼び（`hive_impl.dart:117`）、その後始末が `.lock` を消す（`storage_backend_vm.dart:210-216`）。`openBoxWithRecovery` は壊れた box を最大 3 回開くので、失敗した分の後始末が、最後に開き直した box の `.lock` を後から消しうる。本番は box を閉じないので落ちるのはテストの後片付けだけ。消えた `.lock` の削除失敗だけを吸収し、吸収したら閉じ直して残りの box の失敗を取りこぼさない `closeHiveIgnoringMissingLock` を hive_init_* の 3 ファイルで使う。吸収の範囲は `hive_open_leak_guard_test.dart` が固定する（「何でも吸収」「`contains` へ拡大」「閉じ直さない」の 3 変異で赤）。注: テストで再現したのは**結果の状態**（`.lock` を手で消してから閉じる）で、失敗したオープンと開き直しの競合そのものは起こしていない）
- [ ] L-125 compact が一度失敗すると、その box はそのセッションの間ずっと掃除されない（hive 2.2.3 `storage_backend_vm.dart:147-148` の `_compactionScheduled` が成功時にしか戻らない）。さらに `.hivec` へ書いた後の rename で失敗した場合は閉じた `writeRaf` を持ったままになり、そのセッションは保存もできなくなる（保存を試みた時点で `PersistedBox` がバナーに出す）。失敗した `.hivec` の断片は次に box を開くまで残る。hive 側の挙動で、こちらから旗は戻せない（box を開き直せば戻るが、満杯時は再オープンに失敗して領域ごと失う） — frontend/kotonoha_app/lib/core/persistence/persisted_box.dart の `_removeDeletedFromFile`（#142・#143 の独立監査と Codex。元から）。2026-09-20 追記: この形は `PersistedBox` がセッションの最初の書き込みでも掃除するようになって**到達しやすくなった**（直前の版では delete と clear だけが compact を呼んでいた。put しかしないセッションでも 1 回呼ぶ）。失われるのは「一度失敗した後に空きが戻ったセッションの削除の掃除」で、害はプライバシー（消した発話がファイルに残る）でありデータ喪失ではない。受け入れた理由: 削除しない利用者のセッションは、これが無いと一度も掃除されない（#143 の 2 系統レビューが実測）。決定（2026-09-20、決定シート）: 受け入れて記録する（A）。L-126 と同じく上流の挙動。10 月の棚卸しで代替評価に含める。開き直しての復帰は、満杯時に再オープンが失敗して領域ごと失うため採らない
- [ ] L-126 掃除（compact）は `.hivec` に書いて rename するが、hive は rename の前に fsync しない（`buffered_file_writer.dart:26-32`）。rename 後・こちらの `flush()` 完了前に電源が落ちると box 全体を失いうる（端末のファイルシステム次第。未検証）。削除のたびと、セッションの最初の書き込みで 1 回、この窓が開く。L-120 で「上書きのたびには掃除しない」と決めた理由と同じ窓 — frontend/kotonoha_app/lib/core/persistence/persisted_box.dart、hive 2.2.3 `storage_backend_vm.dart:181-188`（#143 の独立監査と Codex。#142 から）。決定（2026-09-20、決定シート）: 受け入れて記録する（A）。上流（hive 2.2.3）の挙動でこちらから塞げない。**10 月の棚卸しで hive の代替（hive_ce など）を評価する**。別セッション（Hive）で扱う
- [ ] L-127 テストの後片付け（`closeHiveIgnoringMissingLock`）は、`.lock` が消えた box を閉じた形と、**テストが box を閉じ損ねたまま一時ディレクトリを消した形**を区別できない（どちらも `.lock` の `PathNotFoundException` になる）。後者は本当の後片付けの失敗だが、無音になる（box 自体は閉じるので後の状態は綺麗で、失うのは信号だけ） — frontend/kotonoha_app/test/core/utils/hive_open_leak_guard.dart（#144 のレビュー、確信度 中）。決定（2026-09-20、決定シート）: 一時ディレクトリの有無で区別する（A）。テストだけの話で本番には出ない。別セッション（Hive）で実施
- [ ] L-128 `Hive.openBox` の失敗は、本番でも誰も await しない Future のエラーを 1 つ漏らす（`hive_impl.dart:117-118` の `completer.completeError`）。アプリは `PlatformDispatcher.onError` も zone のハンドラも置いていないので、破損を見つけた起動のたびにログへ出る（クラッシュはしない見込み。実機では未確認）。置くかどうか — frontend/kotonoha_app/lib/main.dart（#144 のレビュー、Codex）。決定（2026-09-20、決定シート）: まず実機で確認してから決める（A）。クラッシュするなら対処が要る。別セッション（Hive）で確認
- [ ] L-129 `character_input_tts_test.dart` の TC-E2E-082-001 が headless web で時々、描画中の assert `debugSize == size` で落ちる（`text_painter.dart:1351`、対象は `ai_conversion_button.dart:187` の `Text('AI変換')`）。レイアウト時と描画時で文字の大きさが違うときに出る assert。#144（テストだけの変更で lib は無変更）のマージコミット 597b614e0 で 1 回発生し、同じコードを含む 9ddc49ae9 と手元の headless web では緑。main の直近 15 回で初。再発したら、まず #134 が入れたボタンラベルの倍率がけ（`theme_provider.dart` の `_scaledButtonStyle` と `AnimatedTheme` の 200ms 補間）を疑う — frontend/kotonoha_app/integration_test/character_input_tts_test.dart（2026-09-20、CI run 35479009763）。決定（2026-09-20 第 2 回、決定シート）: 現状維持（A）。再発したら #134 のボタンラベルの倍率がけを疑う。いま追っても再現せず観測点が無い

## 計測（棚卸しの記録。最新の 1 回だけ残す）
| 日付 | 核 | 未卒業 ADR | 台帳 未対応/対応済/却下 | 出力ゼロの仕組み | 実在しないパス参照 | kill rate | 製品/全体（30 日） |
|---|---|---|---|---|---|---|---|
| 2026-09-06（Phase 5 完了・レビュー修正込み） | AGENTS.md 81 行（上限 120） | 5 本（上限 5）。60 行超 0 本 | 27 / 7 / 0 | release.yml（tag 契機で正常）、Stop フック痕跡なし（2026-08-30 設置、廃棄条件の 1 か月は未到達。2026-09-30 に到達、L-79） | 11 件（真に壊れた参照 0 件。ルート名・`.env` ひな形・pre-commit のフック・台帳の解決済み参照・不在の記述・OpenSpec の意図的不在） | 85.9%（158/184。2026-09-05 の測定を再利用、対象コードは無変更。test_sinks.py の差分は docstring のみ） | —（2026-10-01 の棚卸しから） |
