# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/backend/lib/
/backend/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
venv/
env/
ENV/

# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
**/*.g.dart
**/*.freezed.dart
coverage/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment variables
.env
.env.local
.env.*.local

# Database
*.db
*.sqlite
*.sqlite3

# Logs
logs/
*.log

# Docker
docker-compose.override.yml

# OS
.DS_Store
Thumbs.db

# Alembic
alembic/versions/*.pyc

# Test coverage
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.pytest_cache/

# エージェント用の一時 worktree（埋め込みリポジトリとして誤って追加されるのを防ぐ）
.claude/worktrees/

# tsumiki dcs 系コマンドの分析出力（一時的な成果物。コミットしない）
.dcs/

# subagent-driven-development の工程記録（ledger・レビューパッケージ）。
# 完了時に削除する一時的な成果物。
.superpowers/

# OpenSpec は使い捨て（ADR-010）。config.yaml を含め openspec/ 全体を追跡しない（2026-09-08）。
openspec/

# adr-rubber-duck の壁打ちセッション記録（工程記録。恒久成果物は ADR 本体のみ）
docs/adr/.sessions/

# mutmut の作業ディレクトリ・キャッシュ（1回計測用の一時成果物。恒久成果物は docs/ledger.md の記録のみ）
backend/mutants/
backend/.mutmut-cache

# fvm が作る SDK のシンボリックリンク（.fvmrc は追跡する）
frontend/kotonoha_app/.fvm/
